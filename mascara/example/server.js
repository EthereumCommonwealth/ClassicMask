const express=require('express') const createMetamascaraServer=require('../server/') const createBundle=require('../server/util').createBundle const serveBundle=require('../server/util').serveBundle const mascaraServer=createMetamascaraServer() const mascaraPort=9001 mascaraServer.listen(mascaraPort) console.log(`Mascara service listening on port ${mascaraPort}`) const dappServer=express() serveBundle(dappServer,'/app.js',createBundle(require.resolve('./app.js'))) dappServer.use(express.static(__dirname+'/app/')) const dappPort='9002' dappServer.listen(dappPort) console.log(`Dapp listening on port ${dappPort}`)